import __buffer_polyfill from 'vite-plugin-node-polyfills/shims/buffer'
globalThis.Buffer = globalThis.Buffer || __buffer_polyfill
import __global_polyfill from 'vite-plugin-node-polyfills/shims/global'
globalThis.global = globalThis.global || __global_polyfill
import __process_polyfill from 'vite-plugin-node-polyfills/shims/process'
globalThis.process = globalThis.process || __process_polyfill

import {
  require_dist as require_dist4
} from "./chunk-TO24XCBE.js";
import {
  require_postcss
} from "./chunk-QLFGBLE4.js";
import {
  __commonJS,
  __require,
  __toESM,
  require_dist,
  require_dist2,
  require_dist3
} from "./chunk-HG6TCYAD.js";

// node_modules/@tailwindcss/nesting/node_modules/postcss-nested/index.js
var require_postcss_nested = __commonJS({
  "node_modules/@tailwindcss/nesting/node_modules/postcss-nested/index.js"(exports, module) {
    var import_dist = __toESM(require_dist());
    var import_dist2 = __toESM(require_dist2());
    var import_dist3 = __toESM(require_dist3());
    var parser = require_dist4();
    function parse(str, rule) {
      let nodes;
      let saver = parser((parsed) => {
        nodes = parsed;
      });
      try {
        saver.processSync(str);
      } catch (e) {
        if (str.includes(":")) {
          throw rule ? rule.error("Missed semicolon") : e;
        } else {
          throw rule ? rule.error(e.message) : e;
        }
      }
      return nodes.at(0);
    }
    function replace(nodes, parent) {
      let replaced = false;
      nodes.each((i) => {
        if (i.type === "nesting") {
          let clonedParent = parent.clone();
          if (i.value !== "&") {
            i.replaceWith(parse(i.value.replace("&", clonedParent.toString())));
          } else {
            i.replaceWith(clonedParent);
          }
          replaced = true;
        } else if (i.nodes) {
          if (replace(i, parent)) {
            replaced = true;
          }
        }
      });
      return replaced;
    }
    function selectors(parent, child) {
      let result = [];
      parent.selectors.forEach((i) => {
        let parentNode = parse(i, parent);
        child.selectors.forEach((j) => {
          if (j.length) {
            let node = parse(j, child);
            let replaced = replace(node, parentNode);
            if (!replaced) {
              node.prepend(parser.combinator({ value: " " }));
              node.prepend(parentNode.clone());
            }
            result.push(node.toString());
          }
        });
      });
      return result;
    }
    function pickComment(comment, after) {
      if (comment && comment.type === "comment") {
        after.after(comment);
        return comment;
      } else {
        return after;
      }
    }
    function createFnAtruleChilds(bubble) {
      return function atruleChilds(rule, atrule, bubbling) {
        let children = [];
        atrule.each((child) => {
          if (child.type === "comment") {
            children.push(child);
          } else if (child.type === "decl") {
            children.push(child);
          } else if (child.type === "rule" && bubbling) {
            child.selectors = selectors(rule, child);
          } else if (child.type === "atrule") {
            if (child.nodes && bubble[child.name]) {
              atruleChilds(rule, child, true);
            } else {
              children.push(child);
            }
          }
        });
        if (bubbling) {
          if (children.length) {
            let clone = rule.clone({ nodes: [] });
            for (let child of children) {
              clone.append(child);
            }
            atrule.prepend(clone);
          }
        }
      };
    }
    function pickDeclarations(selector, declarations, after, Rule) {
      let parent = new Rule({
        selector,
        nodes: []
      });
      for (let declaration of declarations) {
        parent.append(declaration);
      }
      after.after(parent);
      return parent;
    }
    function atruleNames(defaults, custom) {
      let list = {};
      for (let i of defaults) {
        list[i] = true;
      }
      if (custom) {
        for (let i of custom) {
          let name = i.replace(/^@/, "");
          list[name] = true;
        }
      }
      return list;
    }
    module.exports = (opts = {}) => {
      let bubble = atruleNames(["media", "supports"], opts.bubble);
      let atruleChilds = createFnAtruleChilds(bubble);
      let unwrap = atruleNames(
        [
          "document",
          "font-face",
          "keyframes",
          "-webkit-keyframes",
          "-moz-keyframes"
        ],
        opts.unwrap
      );
      let preserveEmpty = opts.preserveEmpty;
      return {
        postcssPlugin: "postcss-nested",
        Rule(rule, { Rule }) {
          let unwrapped = false;
          let after = rule;
          let copyDeclarations = false;
          let declarations = [];
          rule.each((child) => {
            if (child.type === "rule") {
              if (declarations.length) {
                after = pickDeclarations(rule.selector, declarations, after, Rule);
                declarations = [];
              }
              copyDeclarations = true;
              unwrapped = true;
              child.selectors = selectors(rule, child);
              after = pickComment(child.prev(), after);
              after.after(child);
              after = child;
            } else if (child.type === "atrule") {
              if (declarations.length) {
                after = pickDeclarations(rule.selector, declarations, after, Rule);
                declarations = [];
              }
              if (child.name === "at-root") {
                unwrapped = true;
                atruleChilds(rule, child, false);
                let nodes = child.nodes;
                if (child.params) {
                  nodes = new Rule({ selector: child.params, nodes });
                }
                after.after(nodes);
                after = nodes;
                child.remove();
              } else if (bubble[child.name]) {
                copyDeclarations = true;
                unwrapped = true;
                atruleChilds(rule, child, true);
                after = pickComment(child.prev(), after);
                after.after(child);
                after = child;
              } else if (unwrap[child.name]) {
                copyDeclarations = true;
                unwrapped = true;
                atruleChilds(rule, child, false);
                after = pickComment(child.prev(), after);
                after.after(child);
                after = child;
              } else if (copyDeclarations) {
                declarations.push(child);
              }
            } else if (child.type === "decl" && copyDeclarations) {
              declarations.push(child);
            }
          });
          if (declarations.length) {
            after = pickDeclarations(rule.selector, declarations, after, Rule);
          }
          if (unwrapped && preserveEmpty !== true) {
            rule.raws.semicolon = true;
            if (rule.nodes.length === 0) rule.remove();
          }
        }
      };
    };
    module.exports.postcss = true;
  }
});

// node_modules/@tailwindcss/nesting/index.js
var require_nesting = __commonJS({
  "node_modules/@tailwindcss/nesting/index.js"(exports, module) {
    var import_dist = __toESM(require_dist());
    var import_dist2 = __toESM(require_dist2());
    var import_dist3 = __toESM(require_dist3());
    var postcss = require_postcss();
    var postcssNested = require_postcss_nested();
    module.exports = (opts = postcssNested) => {
      return {
        postcssPlugin: "@tailwindcss/nesting",
        Once(root, { result }) {
          root.walkAtRules("screen", (rule) => {
            rule.name = "media";
            rule.params = `screen(${rule.params})`;
          });
          root.walkAtRules("apply", (rule) => {
            rule.before(postcss.decl({ prop: "__apply", value: rule.params }));
            rule.remove();
          });
          let plugin = (() => {
            if (typeof opts === "function") {
              return opts;
            }
            if (typeof opts === "string") {
              return __require(opts);
            }
            if (Object.keys(opts).length <= 0) {
              return postcssNested;
            }
            throw new Error("@tailwindcss/nesting should be loaded with a nesting plugin.");
          })();
          postcss([plugin]).process(root, result.opts).sync();
          root.walkDecls("__apply", (decl) => {
            decl.before(postcss.atRule({ name: "apply", params: decl.value }));
            decl.remove();
          });
          return root;
        }
      };
    };
    module.exports.postcss = true;
  }
});
export default require_nesting();
//# sourceMappingURL=@tailwindcss_nesting.js.map
